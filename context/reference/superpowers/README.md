# Superpowers design & plan archive

Historical `/10x` design specs (`specs/`) and implementation plans (`plans/`) from shipped
changes — kept for the **rationale** behind decisions, not as current truth. For what the code
does _now_, read the code and the living docs (`context/foundation/lessons.md`, the reviewed
references under `context/reference/docs_reviewed_19_06/`).

**Verify before quoting.** These predate later changes and some claims have drifted. Confirm any
statement against current code before repeating it in a living doc (see the Doc-lifecycle rule in
`AGENTS.md`). Their durable nuggets have already been extracted into the living docs above.

## Still live — not history

- `specs/2026-05-28-kosztorys-in-app-editor-design.md` — the **parked** in-app kosztorys editor
  POC. Still the design of record for an undecided feature; the code is not in this tree. Keep
  until the POC ships or is formally abandoned.

## Shipped — archival

Everything else here documents a change that shipped. `archive/` holds the settled/correction
financials-parity effort; its lesson lives in `context/foundation/lessons.md`.
