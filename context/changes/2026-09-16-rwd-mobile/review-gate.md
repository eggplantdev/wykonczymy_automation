# Review-gate ledger — rwd-mobile · 2026-09-16

Scope: every uncommitted change in the working tree EXCEPT `e2e/` and `src/__tests__/`,
which a parallel agent owns. That includes a second agent's dead-code/refactor work
(notifications, unread badges, reconcile-leads, parse-date-range) sitting in the same tree.

## Findings

- [x] filed EX-787 · feature-first · `ui/data-table/data-table-toolbar.tsx:3-6` · directory-level cycle `ui/ → filters/ → ui/`. The named fix (drop the `search` slot) removes only ONE of the two imports — `FILTER_CONTROL_GRID` keeps the cycle alive — and discards the toolbar's stated slot contract across ~10 tables. The real question is whether `ui/data-table/` belongs in `ui/` at all; deferred to EX-787.
- [x] filed EX-787 · feature-first · `filters/search-filter-input.tsx:11` · same root as the cycle above; `FILTER_CONTROL_GRID` in `filters/filter-grid.tsx` is the same shape, so moving one without the other buys nothing. Rides on EX-787.
- [x] fixed · structure-scatter · `lib/utils/append-short-id.ts:16` · now goes through `randomId()`, the helper that already handles the non-secure-context gap, instead of calling `crypto.randomUUID()` raw. Removes the latent bug and the need to record the server-only assumption.
- [x] fixed · tailwind-v4 · `globals.css` `--spacing-below-top-nav` · one token, both call sites now `h-below-top-nav`; no bare `100dvh-3.5rem` left in `src`. Utility generation verified by compiling a probe through `@tailwindcss/postcss`.
- [x] fixed · tailwind-v4 · `ui/alert-dialog.tsx:39` · comment added naming the 1rem-per-side gutter and why width comes before `max-w-sm` (24rem is wider than a 375px viewport).
- [x] dismissed · tailwind-v4 · `filters/search-filter-input.tsx:11` · not a mis-mapped upstream tier — the constant is new in this slice, written against this app's scale, so there is no upstream `md:` it was copied from. 160px from 768→1280 and 224px above is a deliberate three-step ramp; EX-624 governs re-mapping copied snippets, which this is not.
- [x] fixed · tailwind-v4 · three `--container-dialog{,-sm,-lg}` tokens in `globals.css`; the call sites are `sm:max-w-dialog{,-sm,-lg}`. Raw px converted to rem (420/600/900 → 26.25/37.5/56.25rem), same computed values. All three utilities verified generated.
- [x] filed EX-786 · tailwind-v4 · z-ladder `10000/10001/10002/100000000` across 13 call sites in two spellings; ordering contract lives only in a prose comment in `mobile-nav.tsx`. Deferred out of the slice by the owner — no mobile symptom, and the fix touches files this slice never opened.
- [x] fixed · comment-noise · `lib/queries/transfer-filters.ts:110` · deleted
- [x] fixed · comment-noise · `lib/constants/sections.ts:24` · deleted
- [x] fixed · comment-noise · `ui/data-table/data-table-toolbar.tsx` · `title` / `filters` / `columns` prop docs deleted
- [x] fixed · comment-noise · `ui/data-table/data-table-toolbar.tsx` · placement clauses trimmed off `search` and `actions`; „shared toolbar width" and the action examples kept
- [x] fixed · comment-noise · `app/(frontend)/layout.tsx:55` · rewritten with the `use(promise)` change — restatement and framework narration gone, the staleness contract kept and its mechanism corrected (see the 🔵 below)
- [x] fixed · comment-noise · `nav/mobile-nav.tsx:19` · vanished-state clause cut; the `<main>`-not-`body` and inline-style reasoning kept
- [x] fixed · comment-noise · `lib/queries/unread-counts.ts:15` · rewritten with the auth fix; the short-circuit restatement is gone and the comment now carries the reason the params were removed
- [x] fixed · comment-noise · `fleet/fleet-data-table.tsx:30` · trailing clause cut
- [x] fixed · comment-noise · `kosztorys-editor-body.tsx` · moved down onto the `cn()` it explains, and trimmed — the „viewport minus the h-14 TopNav" half now lives once, on the token in `globals.css`
- [x] fixed · responsive · `nav/mobile-nav.tsx:78` · drawer's `z-10002` is resolved INSIDE `top-nav.tsx:15`'s `sticky z-40` stacking context, so the env badge (`z-10000`, body child) and ToastContainer (`z-10001`) paint over the open drawer's bottom block — exactly where „Wyloguj" sits. CONFIRMED in main thread: no portal, `<nav>` is a header child. Fixed: `<nav>` portalled to `<body>` via radix `Portal.Root asChild`, trigger left in the bar.
- [x] dropped · responsive · `ui/button.tsx:35-39` · a 44px floor on `buttonVariants` was applied and REVERTED on the user's ruling — it changed every button in the app on mobile off one audit finding nobody asked for. The drawer's own hamburger/X keep their inline `size-11`; no app-wide touch-target work in this slice.
- [x] fixed · responsive · `filters/filter-grid.tsx:9` · was `flex flex-wrap`, the layout `DataTableToolbar` had already replaced with a private `GRID_BELOW_SM`. Fixed by hoisting that grid here as the shared `FILTER_CONTROL_GRID`, which the toolbar now imports — one definition, both call sites.
- [x] fixed · responsive · `transfers/transfer-filters.tsx:269,278` · both now use `SEARCH_FILTER_TOOLBAR_WIDTH`, so the two search fields go full-width on a phone instead of sitting at a fixed 160px
- [x] fixed · responsive · `ui/popover.tsx:29` + `ui/dropdown-menu.tsx:36,223` · capped with Radix's own `--radix-*-content-available-width`, which already accounts for the anchor and collision padding — better than a raw `calc(100vw-1rem)`. `w-72` kept as the wanted width; the cap is what stops a caller's `w-96` running off a 375px screen.
- [x] fixed · responsive · `ui/data-table/data-table-toolbar.tsx:12` · `min-w-40` in the 2-col grid → `sm:min-w-40` on the three filter controls (`filters/clear-button.tsx:16`, `filters/filter-trigger-button.tsx:41`, `filters/date-filter-button.tsx:34`). The finding undersold it: page `p-4` + `gap-2` makes each column `(W-40)/2`, so a 160px floor needs W ≥ 360 — 360px is a common Android width, so it IS reachable. The grid cell already sets the width; the floor only ever mattered in the desktop flex row, which is what `sm:` keeps. Rationale recorded on `filters/filter-grid.tsx`.
- [x] dismissed · responsive · `sheets/iframe-view.tsx` · decision, not defect: the „otwórz na komputerze" fallback is gone, phone affordance is now only the „Otwórz w Arkuszach ↗" link. Owner reviewed and accepted — the link IS the phone affordance.
- [x] 🟡 WARNING · fixed · code-review · `app/(frontend)/layout.tsx:56` · the layout's blocking `await` on three DB counts. Owner ruled the counts must be suspended. Provider now carries the pending `Promise<UnreadCountsT>`; `useUnreadCounts()` `use()`s it; `UnreadBadge` wraps the unwrap in `<Suspense fallback={null}>` — a bubble waits, the shell does not. tsc clean.
      test: no automated test — a streaming-boundary change with no assertable state; covered by the manual mobile pass
- [x] 🟡 WARNING · fixed · code-review · `lib/queries/unread-counts.ts:21` · took the `userId`/`role` params away entirely and made the function read its own session via the React-cached `getCurrentUserJwt()` — the trust boundary is removed rather than guarded, so no `'use server'` added later can turn it into "give me anyone's counts". Caller in `layout.tsx` simplified to `fetchUnreadCounts()`. tsc clean.
      test: no automated test — the guard is now structural (there is no identity to pass), so there is no input a spec could get wrong
- [x] 🟡 WARNING · fixed · code-review · `transfers/transfer-filters.tsx:103,134,141` · the default view reported itself as a filter and „Wyczyść filtry" revealed anulowane. Owner confirmed. Root cause was the row being stated backwards: „Ukryj anulowane" describes the resting state, so it was ticked before anyone filtered anything and every count downstream believed it. Now „Pokaż anulowane", active only on a real departure; `hasEntityFilters` keys off `showCancelled`; clear removes the param instead of writing `1`. No spec referenced the old label.
      test: TDD · dom — worth a spec asserting a virgin URL reports zero active filters and that clear restores the default; deferred to the Step-3 test author
- [x] 🔵 · dismissed (owner: deliberate) · code-review · `lib/queries/fleet.ts:54` · `fetchFleetOverview()` lost its `costRange` param and hardcodes `ALL_TIME` while `/flota` still parses `?from=`/`?to=` — the range chip renders active and the „Koszty" column silently ignores it. [other agent's work]
- [x] 🔵 · dismissed · code-review · `hooks/transfers/recalculate-balances.ts:11,47` · premise checked and false. The flag was never "skip sheet write" — that is the separate `skipSheetSync` (`with-payload-transaction.ts:15`). `skipRevalidation` already means "caller is outside a Next request scope, where `revalidateTag` throws" at ~40 sites (`revalidate-collection.ts:21`, `store-lead.ts:47`), so honouring it here is consistent, not a widening. Every setter touching transfers is a seed script or DB spec; the one in-request setter (`sweep-io.ts:57`) writes equipment and revalidates once after its loop. No stale-balance path exists.
- [x] 🔵 · dismissed · code-review · `nav/mobile-nav.tsx` footer · missing `/admin` link — owner ruled the link is no longer wanted anywhere; the commented-out twin in `sidebar.tsx:127` deleted rather than restored.
- [x] fixed · 🔵 · code-review · `nav/mobile-nav.tsx:146` · the one exit that never runs `toggle(false)` — „Wyloguj", where the redirect tears the shell down — now releases the lock explicitly, so correctness no longer rests on `<main>` being remounted
- [x] fixed · 🔵 · code-review · `nav/mobile-nav.tsx:42` · opening now focuses the X inside the panel (`closeRef`), so Escape fires on a node under the `<nav>` and reaches its handler
- [x] 🔵 · dismissed · code-review · `ui/dialog.tsx:52` · the invariant is already written down where a caller meets it — the sheet comment at `dialog.tsx:55-59` ends "A caller overriding width or height must therefore prefix it `sm:`, or it overrides the sheet too." All 8 callers are correct, and the three new `max-w-dialog*` tokens are `sm:`-prefixed at every use. Enforcing it in types would mean a bespoke className parser; not worth it.
- [x] fixed · 🔵 · code-review · `lib/queries/transfer-filters.ts:272` · now recurses into `or`/`and` like both siblings. It is the inverse of `scopeAuditThroughOriginal`, which DOES write `cancelledTransaction.*` keys inside a branch — a nested one survived the strip, reached `buildSqlConditions`, and threw on the dotted path, 500ing the inwestycja/pracownik page instead of dropping a tile.
      test: TDD · unit — the existing spec at `transfer-filters.test.ts:169` asserts the flat case; the nested case wants the same assertion. Deferred to the Step-3 test author.
- [x] dismissed · 🔵 · code-review · `filters/filter-multi-select.tsx:92` · the default is load-bearing, not sloppy: a toggles-only menu has no `values` and no handler (documented at `:21`). Making it a type error means a discriminated union over the whole prop type, rippling to every call site's inference, for a small payoff.
- [x] fixed · 🔵 · code-review · `app/(frontend)/layout.tsx:55` · comment now names the actual mechanism (router-cache reuse within the same segment) instead of claiming the layout renders once per full load
- [x] fixed · 🔵 · code-review · `transfers/transfer-filters.tsx:166` · removed
- [x] ⚠️ · accepted · impl-review · `ui/data-table/data-table-toolbar.tsx` · Phase-4-shaped toolbar refactor (11 files, 259+/318-) landed inside Phase 1–3. Surfaced to the owner with its origin (the ragged wrapped toolbars on a phone) and its size; ruled to keep it in this slice and finish now.
- [x] fixed · impl-review · plan item 2.2 · the plan item was partly wrong and is now corrected in `plan.md` rather than executed. `dropdown-menu.tsx` already carried both collision caps; only `popover.tsx` lacked the height half, now added — the two primitives are symmetric. Dropping `w-72` and `command.tsx`'s `max-h-[300px]` is NOT done and should not be: the first inflates a 288px menu to the anchor's full width on desktop across 29 call sites, the second is a content preference that binds before the collision cap, not a duplicate of it. Redundant duplicate at `filter-multi-select.tsx:268` removed. 2.3 ticked code-complete with the manual-pass caveat.
- [x] ⚠️ · filed EX-786 · impl-review · plan item 1.4 · untouched, and the drawer added a FOURTH ad-hoc z layer (`z-10002`). Owner deferred it out of this slice; plan item 1.4 is marked DEFERRED → EX-786.
- [x] fixed · impl-review · `plan.md` · 1.2 / 1.3 / 3.3 ticked as done; 3.1 / 3.2 ticked **code-complete with the manual-phone-pass caveat recorded** rather than claimed verified — no browser this session. `Progress → Automated` filled with what actually ran, including what did not.
- [x] fixed · impl-review · eight files formatted (`tables/transfers.tsx`, `transfers/transfer-filters.tsx`, `ui/command.tsx`, `ui/dialog.tsx`, `ui/field.tsx`, `ui/input.tsx`, `ui/textarea.tsx`, `hooks/transfers/recalculate-balances.ts`); `prettier --check` now clean across the slice's non-test files
- [x] dismissed · impl-review · commit-splitting · owner's ruling (2026-09-16): „I don't care how you split commits." The split is the agent's call, so this is no longer a question the gate owes anyone. Recorded for the record: the other agent's unread-badge unification is a DEPENDENCY of plan item 1.2 and rides in the same commit as the drawer; `cancelled-filter-button.tsx` / `cancelled-transaction-audit-button.tsx` are THIS slice's deletions, not the other agent's.

- [x] filed EX-788 · gate · E2E obligation · a browser-level slice owes its E2E; deferred rather than authored, per the gate's file-or-author rule. Covers the nav drawer at a phone viewport (the „Wyloguj" paint-order bug is the regression guard), adding a transaction as a full-height sheet, and no horizontal page scroll at 360px. Labelled `e2e-backlog`.
- [x] fixed · simplify · `lib/queries/transfer-filters.ts:200-300` · three hand-written walks of the same `Where` tree collapsed into `mapWhereLeaves` / `someWhereLeaf`; each walker now supplies only its per-field rule. The branch recursion that the nested-`or` 500 fix introduced is now in ONE place instead of three that had to be kept in step by hand.
- [x] fixed · simplify · `lib/queries/transfer-filters.ts` + `inwestycje/[id]/page.tsx:48` + `pracownicy/[id]/page.tsx:37` · both pages spelled out `stripCancelledFilters(stripOriginalScopedFilters(...))` verbatim, comment included, and a third would have had to know the order. Now one exported `statsWhereFrom(where)`.
- [x] fixed · simplify · `globals.css` `@utility typing-surface` + `ui/input.tsx:15`, `ui/textarea.tsx:10`, `ui/command.tsx:37`, `ui/datasheet-grid/editable-cell-input.tsx:47` · the iOS-zoom incantation (`text-base … placeholder:text-sm … sm:text-sm`) was pasted in three places and the fourth typing surface — the kosztorys cell — had silently missed it, so editing a cell on iOS still zoomed the page. One utility, four call sites, the miss closed. Generation verified by compiling a probe through `@tailwindcss/postcss` (the `sm` half resolves to this app's 48rem, not upstream's 40).
- [x] fixed · simplify · `nav/nav-link-item.tsx` (new) · the nav-link row was duplicated between `nav/mobile-nav.tsx:109-131` and `nav/sidebar.tsx:74-95`, active-class string verbatim included — the two render the same link and must stay indistinguishable. `BadgeSlot` moved here with it.
- [x] fixed · simplify · `nav/logout-button.tsx` (new) · same duplication for „Wyloguj" (`mobile-nav.tsx:143-158` / `sidebar.tsx:134-145`), each with its own `useTransition`. The drawer's scroll-lock release rides in as `beforeLogout`.
- [x] fixed · simplify · `filters/filter-multi-select.tsx:176-181` · the `triggerCount == null ? … : …` branch was written twice, once for the highlight and once for the badge. Hoisted to `isFiltered` / `triggerBadgeCount` — still two expressions, because „nic nie zaznaczone" is a filter that counts zero and must light up anyway; that is now stated once instead of inferred twice.
- [x] fixed · simplify · `equipment/held-equipment-section.tsx:12` · its third track was a bare `9rem` and so could not squeeze on a phone — the exact miss `SUMMARY_VALUE_COL` exists to prevent. Now uses the token.
- [x] fixed · simplify · `ui/active-filter-button.tsx` · `disabled?: boolean` had zero callers; deleted. The component now routes through `FilterTriggerButton`, which also fixes the geometry mismatch where it sat in the filter grid beside controls with a different width and alignment.
- [x] fixed · simplify · `filters/clear-button.tsx`, `filters/date-filter-button.tsx`, `ui/active-filter-button.tsx` · three filter controls each re-built the trigger's shape by hand; all three now render `FilterTriggerButton`, which is the single home of the shared `sm:min-w-40` floor.
- [x] fixed · simplify · `ui/data-table/data-table-toolbar.tsx` · both group wrappers now render `<FilterGrid>` instead of importing its class constant, so `FILTER_CONTROL_GRID` is un-exported and lives in one file. `sm:flex-initial` (not `flex-none`) preserves the toolbar's previous desktop wrapping exactly.
- [x] fixed · simplify · `transfers/transfer-filters.tsx` · `showCancelled` had a hand-written branch beside the generic `ENTITY_FILTER_KEYS` path for both „are there filters" and „clear filters"; folded into the list, since revealing anulowane IS a departure from the default view.
- [x] fixed · simplify · `filters/filter-multi-select.tsx:270` · `w-72` and the height cap removed — both are the popover primitive's job now, and the duplicate only obscured that the `overflow-hidden` beside them is the deliberate override.
- [x] fixed · simplify · `ui/command.tsx:50` · comment naming why the 300px cap and the collision cap are two caps with two jobs, not a duplication to collapse.
- [x] dismissed · simplify · `ui/dialog.tsx:98` · premise false. The finding said only 2 of 18 dialogs render the „Wyczyść formularz" button that `pr-24` reserves room for; in fact 13 forms go through `FormShell`, which always renders it, plus `edit-transfer-form`. The reservation is right for the majority.
- [x] dismissed · simplify · `ui/summary-grid.tsx:18-20` · premise false. `min(7rem, 24vw)` is a FLOOR, not a breakpoint approximation — above ~467px the rem is simply the smaller of the two and the floor is a flat 7rem, which is what the existing comment already says. Nothing to align to 768.
- [x] dismissed · simplify · `ui/data-table/data-table.tsx:175,179` · `max-sm:space-y-6` does not re-implement `PageWrapper`'s `gap-6` — the table is ONE grid item; this spaces its own children, deliberately to the same value, and the comment says so. `max-sm:-mt-4` pulls the count row to 8px, it does not cancel the 24px.
- [x] dismissed · simplify · `kosztorys/summary/tables/materials-transactions-table.tsx:73-83` · a `truncatedCell(maxWidth)` factory would have to take the element (`span` vs `button`), the text derivation (`getValue` vs `firstNoteLine`) and the focus classes — the parameters are the code. The two cells differ for reasons each records.
- [x] dropped · simplify · `transfers/transfers-section.tsx:7` · restoring the `title` default to remove three `"Transfery"` literals needs a `null` sentinel for the one heading-free caller. The explicit prop reads better than the sentinel; not worth the trade.
- [x] dropped · simplify · `filters/filter-multi-select.tsx:88-95` · `options = []` / `onValuesChange = () => {}`. Making them required just relocates the no-op to the one toggles-only call site. Same conclusion the code-review finding at the top of this list reached independently.
- [x] dropped · simplify · `filters/filter-grid.tsx:14` · `[&_button]` → `[&>button]`. Popover content is portalled, so the descendant over-reach is largely theoretical, and the child combinator would silently drop `justify-start` from any wrapped control. Not worth the churn without a browser.
- [x] dropped · simplify · `lib/actions/workers.ts:17` · routing `crypto.randomUUID()` through `randomId()`. Verified: `random-id.ts`'s fallback is a real v4 off `crypto.getRandomValues`, so there is no security gap either way, and the file is `'use server'` — the non-secure-context case it guards cannot arise.
- [x] skipped · simplify · `ui/dialog.tsx` · a `size` prop on `DialogContent`. 16 of 18 callers sit on stock `sm:max-w-md/lg/2xl/3xl/4xl/5xl`; a three-token scale cannot reproduce six widths, so adopting it is behavior-changing and wants a browser. Real, but not this slice.
- [x] skipped · simplify · `lib/fleet/rows.ts:38,55` · `costRange` is now a dead param in production. Other agent's domain, it removes a capability, and it deletes specs the parallel test agent owns. Already dismissed as deliberate under the code-review finding above.
- [x] skipped · simplify · `filters/filter-multi-select.tsx:313-327` · two `actionRows` layouts for one caller. Collapsing them drops the „Sekcje" caption, which is a visible behavior change.
- [x] dismissed · simplify · efficiency pass · returned clean over the whole 96-file / 3951-line diff — no redundant computation, sequential independent I/O or closure-captured long-lived objects introduced.

- [x] skipped · gate · manual phone pass · owner deferred it to a later session ("manual checks later"). 3.1 / 3.2 / 2.3 stay ticked as code-complete with the caveat recorded in `plan.md`, not claimed verified.

## Simplify pass

Ran /simplify as a 4-agent read-only fan-out (reuse / simplification / efficiency / altitude) over
`git diff HEAD -- src/ ':(exclude)src/__tests__/'` — 96 files, 3951 lines. 25 findings after dedup:
**13 applied, 0 proposed, 6 dismissed, 3 dropped, 3 skipped.** Each is folded into `## Findings`
above, tagged `simplify`. Report: see the path printed at the end of the run.

## Tests & suite

- `pnpm exec tsc --noEmit` (excluding pre-existing `e2e/` errors, owned by a parallel agent) — clean
  after every batch, including the last.
- `pnpm exec prettier --check` on every file this gate touched — clean.
- Tailwind generation for the new `@utility typing-surface` verified by compiling a probe through
  `@tailwindcss/postcss`, not assumed.
- Unit/DOM suite: owned by the parallel test agent, not run here. Two test obligations recorded
  above (`transfer-filters` nested-branch case, the virgin-URL filter-count spec) are theirs.
- `pnpm test:e2e`: not run (~1h, on request only). The slice's E2E obligation is filed as EX-788,
  labelled `e2e-backlog`.
