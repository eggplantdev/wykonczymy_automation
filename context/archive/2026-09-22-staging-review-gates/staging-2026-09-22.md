# Review-gate ledger — całe drzewo (staging) · 2026-09-22

Zakres: brudne drzewo na `staging`, 95 plików — unia kilku slice'ów (rename
`lib/invoices` → `lib/media`, wspólny stub `cache-revalidate`, odświeżony golden
master, fix EX-850 w `use-media-upload.ts`).

`/10x-impl-review` odpadł: nie ma jednego `plan.md` kotwiczącego tę unię.
Fan-out: `/code-review` · `tailwind-v4-audit` · `feature-first-structure` ·
`module-cohesion-audit` · `structure-scatter-audit` · `comment-noise-audit`.

## Findings

- [x] 🟡 WARNING · fixed · `code-review` · `context/changes/2026-09-22-zakladka-inwestycja-w-panelu/review-gate.md:1` · zabłąkane `Jak` przed `#` — nagłówek H1 renderuje się jako akapit
      test: no automated test — literówka w dokumencie, nie w kodzie
- [x] 🔵 OBSERVATION · fixed · `code-review` · `src/lib/media/file-archive.ts:108` · `buildArchiveMessage` sparametryzowane przez `copy` wszędzie poza „bez faktury", wbitym na sztywno — dziś nieosiągalne, ale to dokładnie ten błąd rejestru, który ten slice usuwał
      test: TDD · unit — `file-archive.test.ts` istnieje; nowe pole `ArchiveCopyT` dostaje przypadek z `FILE_ARCHIVE_COPY`
- [x] 🔵 OBSERVATION · fixed · `code-review` · `src/__tests__/lib/media/delete-unreferenced-media.test.ts:26` · atrapa `payload.find` ignoruje `pagination`, więc nic nie pinuje `pagination: false` — jego utrata kasuje pliki wciąż przypięte (Blob bez undelete)
      test: TDD · unit — asercja `expect.objectContaining({ pagination: false })` w istniejącym teście batcha
- [x] 🔵 OBSERVATION · fixed · `code-review` · `src/__tests__/components/dialogs/media-preview-dialog.test.tsx:9` · mock zwraca `isZipping` (klucz-widmo po `useInvoiceZip`) i gubi `download`
      test: no automated test — poprawka jest w samym mocku
- [x] 🔵 OBSERVATION · fixed · `code-review` · `AGENTS.md:384` · „31 specs" — faktycznie 34 importują stub
- [x] 🔵 OBSERVATION · fixed · `code-review` · `.gitignore:106` · `.review-gate/` nie było ignorowane — dopisane
- [x] fixed · `feature-first` + `scatter` (zbieżnie) · `src/lib/invoices/` · resztówka po niedokończonym przeniesieniu: 9 plików wyszło, 2 zostały bez reguły, która by je tam trzymała; `row-file-positions.ts` nie zna faktur, a `invoice-field.ts` już importuje w górę z `lib/media/`. Przenieść oba + 2 specki, skasować katalog, poprawić 5 importów
- [x] fixed · `module-cohesion` + `scatter` (zbieżnie) · `src/lib/media/archive-copy.ts:7` · `ArchiveCopyT` deklarowany w module stałych, a `src/types/media.ts:1` importuje go w górę, żeby złożyć `PreviewLabelsT` — zależność biegnie odwrotnie
- [x] fixed · `scatter` · `src/components/media/preview-labels.ts` vs `src/lib/media/archive-copy.ts` · dwa domy dla polskich stałych tekstowych tej samej funkcji, po dwóch stronach granicy `components/` ↔ `lib/`; `preview-labels.ts` ma 8 konsumentów w 6 katalogach, więc nie jest prywatnym kontraktem komponentu
- [x] skipped · `module-cohesion` · `src/lib/media/file-archive.ts:1-127` · grab-bag: projekcja wiersz→plik + budowa nazw plików + składanie polskiego komunikatu. 9 eksportów, trzy powody do zmiany — rozbicie na trzy moduły rusza 6 plików i jeden spec o 29 przypadkach, czyli refaktor na własną bramkę; zostaje po tej rundzie
- [x] dismissed · `module-cohesion` · `src/components/dialogs/media-preview-dialog.tsx:87-132` · `handlePrint` — rozdzielenie już zrobione: `openPrintWindow`/`printThenClose` siedzą w `lib/utils/print-window`, a to, co zostało, to składanie `<img>`/`<iframe>` z `files` i `labels` tego dialogu; wyniesienie zostawiłoby funkcję z pięcioma parametrami tego samego komponentu
- [x] fixed · `scatter` · `src/components/dialogs/invoice-upload-dialog.tsx` · rename zrobiony na połowie pary: bliźniak podglądu poszedł na `Media*`, ten został `Invoice*`, choć 2 z 3 jego konsumentów to assety inwestycji, nie faktury
- [x] fixed · `feature-first` + `scatter` · `src/components/dialogs/media-preview-button.tsx:16`, `media-preview-trigger.tsx:26` · komponent uogólniony, słownictwo nie: prop nazywa się `invoices`, a etykieta mówi „Podgląd faktury" nad zdjęciami z budowy i rzutami
- [x] fixed · `comment-noise` · 2× „upload-invoice is no longer called" (`bulk-transaction.test.ts:43`, `transfer-actions.test.ts:81`) nad nieistniejącym mockiem + 3 komentarze czytające nazwy pól w `ArchiveTallyT` — skasowane
- [x] fixed · `comment-noise` (przebieg 2, po `/simplify`) · 7 bloków · `revalidate-collection.ts:12` i `delete-unreferenced-media.ts:61` — archeologia usuniętego `await`/`entityTag(slug, …)` przycięta do zdania niosącego powód; `use-file-archive.ts:34` powtarzał nagłówek modułu; `investment-assets.db.test.ts:121` powtarzał asercję poniżej (skasowany); 3 dalsze przycięte z narracji o poprzednim stanie
- [x] dismissed · `comment-noise` · „15 banerów powtarzających `describe()`" · sprawdzone jeden po drugim w drzewie diffu: każdy niesie powód istnienia testu (EX-448, EX-597, EX-849, 40P01), żaden nie przechodzi STRIP TESTU jako restatement — drugi, diff-scope'owy przebieg audytu nie zgłosił ani jednego z nich
- [x] dropped · `simplify` · `src/__tests__/transfer-actions.test.ts:40`, `hooks/transfers/delete-invoice-media.test.ts:9` · „dwie atrapy `payload.find`" to dwa `vi.fn()` o różnym kształcie z podobnym komentarzem — wspólne jest zdanie, nie kod; wyniesienie nic nie oszczędza
- [x] fixed · `comment-noise` · 1 blok · `src/__tests__/transfer-actions.test.ts:981` · przyciąć do zdania o koordynacji międzyplikowej; reszta powtarza tytuł `it()`
- [x] fixed · `comment-noise` · `src/__tests__/lib/actions/mirror-workshop-preset.test.ts:11`, `src/components/forms/investment-form/investment-form.tsx:147` · komentarze po polsku — reguła mówi: komentarze zawsze po angielsku (ten drugi niesie realne ograniczenie, więc przepisać, nie kasować)
- [x] fixed · `simplify` · `src/hooks/use-file-archive.ts:52-93` · `download` i `downloadFiles` powielały cały przebieg toasta i tally, rozjeżdżając się na tonie tego samego komunikatu `copy.empty` ('info' vs 'error'); jedna ścieżka `pack()`, dwa cienkie wejścia, pusty zestaw zawsze informacyjny
- [x] fixed · `simplify` · `src/hooks/use-file-archive.ts` · nazwa archiwum składana u trzech wołających (`today()` + `buildArchiveName` + `copy.prefix`); hook bierze `nameParts` i robi to sam
- [x] fixed · `simplify` · `src/lib/cache/tags.ts:24` · `entityTag(collection: string)` nie łapał literówki, która jest dokładnie błędem EX-849; pierwszy parametr zawężony do unii `'investment' | 'cash-register'`
- [x] fixed · `simplify` · `src/lib/actions/lead-assets.ts:68,76` · ręcznie przepisany cel `{collection:'investments',field:'assets'}` i literał `entityTag('investment', …)` — wspólne `investmentAssetsField` (`lib/media/set-upload-field.ts`) i `investmentAssetTags` (`lib/cache/tags.ts`); pisarz, który przegapi tag, to trwale nieświeża galeria
- [x] fixed · `simplify` · `src/lib/actions/run-action.ts:61` · zewnętrzny `if` powtarzał warunki obu gałęzi w środku
- [x] fixed · `simplify` · `src/lib/media/file-archive.ts:76` · `ArchiveTallyT` eksportowany bez konsumenta — zdjęty `export`
- [x] dropped · `code-review` · `src/__tests__/transfer-actions.test.ts:14`, `investment-assets.db.test.ts:20` · atrapy `after` gromadzą nieobserwowane promise'y — `deleteUnreferencedMedia` nie może dziś odrzucić (każda ścieżka w `try/catch`), więc martwe; nie warte zmiany kształtu atrap
- [x] dismissed · `code-review` · `src/__tests__/fixtures/financial-golden-master.json` · stan `MM` to wyłącznie zwijanie tablic przez Prettiera — po normalizacji `sort_keys` bajt w bajt identyczne, żadna kwota się nie ruszyła. Działanie należy do commita (`git add` przed), nie do kodu
- [x] dismissed · `feature-first` · `src/hooks/use-file-archive.ts:53` · `download()` wbija `INVOICE_ARCHIVE_COPY` — uzasadnione w pliku (obaj wołający pakują faktury), a poprawka „bez faktury" i tak domyka ryzyko
- [x] dismissed · `feature-first` · `src/lib/media/blocked-files-message.tsx` · jedyny `.tsx` pod `lib/` — react-toastify zjada `\n`, więc komunikat wielopozycyjny musi być elementami; sprzed tej zmiany, sam rename katalogu
- [x] dismissed · `tailwind-v4-audit` · całość · 0 findingów w 13 plikach `.tsx`; jedyne trafienie `max-h-[90vh]` to cytat w komentarzu, nie klasa
- [x] dismissed · `code-review` · stub vs moduł · `src/__tests__/stubs/cache-revalidate.ts` eksportuje dokładnie te trzy funkcje co `src/lib/cache/revalidate.ts`; zero ocalałych ręcznych fabryk `vi.mock`
- [x] dismissed · `code-review` · tagowanie cache · usunięcie `entityTag(slug, doc.id)` z `revalidate-collection.ts` zweryfikowane jako no-op (hook sypał `investments:6`, każdy konsument czyta `investment:6`); pięciu pisarzy `investments.assets` policzonych, wszyscy tagują poprawnie
- [x] dismissed · `code-review` · rename · `tsc` czysty, eslint cichy, zero żywych odwołań do 12 martwych nazw w `src/`, `e2e/`, `scripts/`; mirrory specek poszły za źródłami
- [x] dismissed · `code-review` · `src/lib/kosztorys/margin-v2.ts` · zmiana wyłącznie komentarzowa, jej teza sprawdzona (oba żywe konsumenty istnieją, konsumenta `/raporty` nie ma)

## Simplify pass

Ran /simplify (4 agenty) — findingi zwinięte do `## Findings` z tagiem `simplify`.

## Tests & suite

Nowe specki (krok 3): `file-archive.test.ts` — „qualifies an empty row in the caller's own words"
(pole `rowWithoutFile`); `delete-unreferenced-media.test.ts` — „spares every attached file in a batch
larger than one page" (atrapa `find` honoruje teraz `pagination`; instrument zwalidowany: po usunięciu
`pagination: false` ze źródła pada dokładnie ten jeden przypadek, źródło przywrócone).

- `pnpm typecheck` — czysto
- `pnpm lint` — 0 errors, 85 warnings (wszystkie z `src/migrations/`, sprzed tej zmiany)
- `pnpm test` — 3866 passed, 331 skipped (404 pliki); 0 failed
- `pnpm test:e2e` — NIE uruchamiane (≈1h, tylko na wyraźną prośbę)
- `pnpm build` — nie uruchamiane
