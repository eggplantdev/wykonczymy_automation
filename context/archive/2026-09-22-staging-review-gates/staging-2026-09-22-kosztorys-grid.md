# Review-gate ledger — kosztorys grid: Sekcja / price / warsztat · 2026-09-22

Zakres: brudne drzewo na `staging`, 10 plików — unia trzech wątków:

1. „Sekcja" jako kolumna zawijana i mierzona do wysokości wiersza (+ bramka na
   fladze edycji dsg, + `close()` w `useInlineRename`),
2. „Cena j.m. netto" (`price`) obecna we WSZYSTKICH widokach, nie tylko klienckim,
3. lista kolumn warsztatu niesie stawkę obok źródła ceny (`ALL_PLANE_PRICE_KEYS`).

Fan-out: `/10x-impl-review` (kotwica: `context/changes/2026-09-22-szablon-autosave/plan.md`) ·
`/code-review` (high) · `tailwind-v4-audit` (0 findingów) · `feature-first-structure` (0) ·
`module-cohesion-audit` (0 nowych) · `structure-scatter-audit` · `comment-noise-audit`.

## Findings

- [x] 🔴 CRITICAL · fixed · `impl-review` · `src/lib/kosztorys/column-config.ts:251` · stawka wykonawcy wraca do `WORKSHOP_VISIBLE_COLUMNS`, odwracając decyzję właściciela zapisaną jako `[x] fixed` w `context/changes/2026-09-22-szablon-autosave/review-gate.md:85-101` („Stawka wykonawcy **nie** wchodzi… Lista warsztatu ma teraz 7 kolumn"). Nowy komentarz nie cytuje właściciela — w odróżnieniu od sąsiada o `priceGross` i bliźniaczego komentarza w spec. Sam argument inżynierski zweryfikowany niezależnie i trzyma się (`serialize-preset.ts:22-33` nie zeruje `wToolsOverrideValue`/`ownToolsOverrideValue`, więc nadpisana stawka jedzie na szablonie). **Kod prawdopodobnie dobry, rejestr sprzeczny** — czeka na potwierdzenie właściciela; box zamyka się po dopisaniu decyzji w `review-gate.md` + `change.md`
      **Dowody dobrane 2026-09-23:** (a) `change.md:31-32` NOTUJE już odwrót („a stawka wykonawcy wróciła”) i w tym samym zdaniu oddaje wiążący głos `review-gate.md` — który mówi coś przeciwnego; rejestr jest sprzeczny sam ze sobą, nie tylko z kodem. (b) Powód właściciela („startuje schowana, a warsztat nie ma pickera, żeby ją przywrócić”) jest w warsztacie **mechanicznie nieprawdziwy**: `selectV2Columns` (`column-selection.ts:40-80`) traktuje zamkniętą listę jako sufit I podłogę, więc nadpisuje `DEFAULT_HIDDEN_COLUMNS` — kolumna z listy warsztatu nie startuje schowana i picker nie jest do niczego potrzebny. (c) EX-820 (`stawka-problems-and-filters`) NIE rości sobie tej zmiany; przeciwnie, prostuje dryf komentarzy twierdzących, że warsztat chowa przełącznik widoku. Decyzja nadal należy do właściciela.
      **Rozstrzygnięte (właściciel, 2026-09-23): kod zostaje, rejestr poprawiony.** Stawka wykonawcy jednak wchodzi, obie płaszczyzny — lista warsztatu ma 9 kolumn. Odwrócenie dopisane do `context/changes/2026-09-22-szablon-autosave/review-gate.md` jako wpis „F6 ODWRÓCONE", a `change.md` przestało odsyłać do zapisu, który mówił coś przeciwnego.
      test: no automated test — to rozjazd dokumentacji, spec już pinuje kod
- [x] 🟡 WARNING · fixed · `code-review` · `src/components/kosztorys/editor/grid/cells/section-name-cell.tsx:70-84` · `SectionNameGridCell` wchodzi w model klawiatury dsg (`focus`), ale nie przyjmuje ani nie przekazuje `stopEditing` — Enter/Escape zostawiają siatkę w trybie edycji nad rozogniskowanym inputem i połykają kolejne znaki. Kontrakt spisany wprost w `use-cell-draft.ts:104-116`; `decimalColumn` i `longTextColumn` go honorują, ta komórka nie. Regresja wprowadzona przez ten diff: przed nim input renderował się bezwarunkowo bez `focus`, więc nieaktualna flaga `editing` była nieszkodliwa
      test: test-driven-debugging · dom — czerwony spec na Enter/Escape w `section-name-cell.test.tsx`
- [x] 🟡 WARNING · fixed · `impl-review` F3+F10 · `src/components/kosztorys/editor/grid/kosztorys-v2-columns.tsx:111-128` · `price` we wszystkich widokach przekracza guardrail planu („**Nie ruszamy edytora inwestycji**", `plan.md:80-81`) — trzeci wyjątek od reguły, która wymieniała dwa. Wyciek zweryfikowany i NIE zachodzi (`assertDisclosurePair` przypina `previewVisible` do widoku klienta; `PREVIEW_VISIBLE_COLUMNS` matchuje pełne id). Skutek uboczny: sort po `price` przeżywa teraz przełączenie widoku (`use-kosztorys-editor.ts:511`). **Rozstrzygnięte (właściciel, 2026-09-23): guardrail był nieprawdziwy, nie kod.** Ten slice faktycznie zmienia widok w edytorze inwestycji i tak ma zostać. Punkt „Nie ruszamy edytora inwestycji" skreślony w `plan.md` z powodem, decyzja dopisana do „Decyzje właściciela" w `change.md` — dotąd jedynym jej zapisem był komentarz w kodzie.
      test: no automated test — decyzja zakresowa, nie zachowanie
- [x] 🟡 WARNING · dismissed · `code-review` · `src/components/kosztorys/editor/grid/kosztorys-v2-columns.tsx:120-127` · `price` ląduje w widokach ekip w pełni EDYTOWALNY i pod nieukwalifikowaną etykietą „Cena j.m. netto", podczas gdy dwie stawki obok niosą sufiks płaszczyzny. Omyłkowa edycja w gołej kolumnie przelicza obie ekipy naraz — to jest mianownik, z którego obie stawki się wywodzą. `columnLabelForView` już rozróżnia `net`/`gross`/`stageQtySum` per widok, więc gołe `price` jest niespójne z konwencją własnego pliku. **Zmienia to, co użytkownik MOŻE zrobić — nie stosuję automatycznie.** **Rozstrzygnięte (właściciel, 2026-09-23): zostaje edytowalna, etykieta zostaje goła.** Ta cena nie ma płaszczyzny, więc sufiks byłby kłamstwem, a blokada do odczytu zabrałaby jedyne miejsce, w którym da się ją poprawić po przełączeniu widoku. Zapisane w `change.md`.
      test: no automated test — świadoma decyzja o zachowaniu, nie regresja
- [x] 🟡 WARNING · dismissed · `impl-review` F4 · `src/lib/kosztorys/row-content-lines.ts:4-8` (+4 pliki) · cały blok „Sekcja zawija się i podnosi wiersz” nie jest zakotwiczony w żadnym planie ani folderze zmiany — 5 z 10 plików diffu. Sama instalacja poprawna i kompletna (`headerClassName` czyni kolumnę mierzalną, `clipCueClass` iteruje `WRAPPING_COLUMN_IDS`, pasma nie puchną przez `resolveRowHeight`). **Rozstrzygnięte — to praca tego slice'u, nie obcy wtręt.** Dwa niezależne łańcuchy: (1) „Sekcja” stoi w `DEFAULT_HIDDEN_COLUMNS` (schowana na zwykłym kosztorysie) i równocześnie w `WORKSHOP_VISIBLE_COLUMNS` — warsztat jest JEDYNYM ekranem, który tę kolumnę wymusza i nie ma pickera, żeby ją zdjąć, więc dopiero `szablon-autosave` postawiło ją jako realnie używaną komórkę siatki; (2) sama mechanika zawijania to nie nowa instalacja, tylko przyrost do wysłanej już linii (`442f9f05` → `b7b60e77`), która nigdy nie miała własnego folderu zmiany — nie ma więc dokąd tej pracy przekotwiczać.
      test: no automated test — rozstrzygnięcie zakresowe, nie zachowanie
- [x] 🔵 OBSERVATION · fixed · `code-review` + `impl-review` F5 · `src/components/kosztorys/editor/hooks/use-inline-rename.ts:52` · nowy commit-na-unmount po cichu zmienia drugiego, nietkniętego konsumenta: `stage-header.tsx:60` zapisuje teraz nazwę etapu, gdy nagłówek znika mid-rename (wirtualizacja kolumn, zmiana `shownStages`, filtr). Wyczyszczenie pola i unmount zapisuje `''` → `label: null`. Zachowanie prawdopodobnie zamierzone (to ten sam argument, co dla komórki), ale nieasertowane
      test: TDD · dom — spec pinujący unmount `StageHeader` mid-rename
- [x] 🔵 OBSERVATION · fixed · `code-review` + `impl-review` F7 · `src/components/kosztorys/editor/grid/cells/section-name-cell.tsx:94` · `kosztorys-section-name-cell` to martwa klasa — jedno wystąpienie w repo (linia, która ją emituje), zero selektorów w `globals.css` — a komentarz nad nią twierdzi, że stylesheet z niej korzysta. Diff przepisuje dokładnie ten `cellClassName`, więc to najtańszy moment na usunięcie
- [x] 🔵 OBSERVATION · fixed · `impl-review` F8 + `scatter` · `src/components/kosztorys/editor/kosztorys-editor-body.tsx:319` · dwie połówki kontraktu CSS budowane w różnych domach: `kosztorys-wrap-<id>` ma nazwany helper `wrapColumnClass`, bliźniak `kosztorys-clipped-<id>` jest gołym literałem. Dodanie trzeciego id wymagało zapamiętania zgody w dwóch niepowiązanych miejscach — następna kolumna w `WRAPPING_COLUMN_IDS` skompiluje się, zmierzy i pojedzie bez sygnału przycięcia
- [x] 🔵 OBSERVATION · fixed · `impl-review` F6 · `src/lib/table/column-order.ts:43-45` · komentarz ilustruje regułę przykładem, który ten diff unieważnił („Inwestor" składa cenę klienta na obu stawkach, widoki podwykonawców tylko te drugie). Sama reguła stoi, umarł tylko przykład
- [x] 🔵 OBSERVATION · fixed · `impl-review` F2 · `context/changes/2026-09-22-szablon-autosave/change.md:29-31`, `plan.md:331-333` · oba dokumenty wciąż deklarują `priceGross` na liście warsztatu; kod go wyklucza i ma rację — decyzja właściciela jest zapisana, tylko w `review-gate.md:96-100`, nie w dokumentach, po które sięga się pierwsze. Te same dwa dokumenty milczą o `actions`
- [x] 🔵 OBSERVATION · fixed · `code-review` · `src/lib/kosztorys/column-config.ts:237-248` · zapisany powód wykluczenia `priceGross` stosuje się tak samo do stawki, którą diff właśnie dodał: NIEnadpisana stawka to `clientPrice × globalWToolsCoeff`, czyli figura wywiedziona z tych samych `settings`, które preset zatrzymuje, ale ignoruje przy aplikacji. „Both travel" trzyma się wyłącznie dla nadpisanej. Mechanicznie zmiana poprawna — przeszacowane jest uzasadnienie
- [x] 🔵 OBSERVATION · fixed · `impl-review` F9 · `context/foundation/manual-checks.md:579-597` · sekcja `szablon-autosave` nie ma checku na to, co ten diff zmienia: warsztat renderuje teraz 9 kolumn (w tym dwie stawki), a „Sekcja" jest tam podłogowo widoczna — więc długa nazwa sekcji podnosi KAŻDY wiersz tej sekcji na jedynym ekranie, który nie może tej kolumny schować
- [x] fixed · `comment-noise` · `src/lib/kosztorys/column-config.ts:233` · zdanie zamykające argumentuje za kodem przez opis stanu SPRZED edycji („Listing the source without the kwota **left** …") — dokładnie ten anti-pattern; oba fakty stoją już w czasie teraźniejszym zdanie wyżej
- [x] fixed · `comment-noise` · `src/components/kosztorys/editor/grid/kosztorys-v2-columns.tsx:112` · to samo: „a crew view that **dropped** it asked the owner to read a verdict without its denominator" broni tablicy przez opis skasowanego ternary
- [x] fixed · `comment-noise` · `src/components/kosztorys/editor/grid/kosztorys-v2-columns.tsx:113` · „Nothing leaks — …" to echo identycznego zdania jedenaście linii wyżej (`:102`)
- [x] fixed · `comment-noise` · `src/lib/kosztorys/row-content-lines.ts:6` · ogon „— the price of the rule holding without exceptions, and only while the column is actually shown" nie nazywa konsekwencji (argumentuje przeciw nienazwanej alternatywie) i powtarza bramkę `widths` udokumentowaną w tym samym pliku
- [x] fixed · `comment-noise` · `src/components/kosztorys/editor/grid/cells/section-name-cell.tsx:95` · dopisane dwie linie o `wrapColumnClass` powtarzają doc samej funkcji w miejscu jej wywołania
- [x] dismissed · `comment-noise` · `src/__tests__/.../section-name-cell.test.tsx:138` (zdanie 1.) · restatement komentarza z SUT, ale audytor sam oznaczył to jako judgment call; drugie zdanie niesie orientację w zakresie, której źródło nie daje — w specu „co jest złe bez tej asercji" jest zadaniem komentarza
- [x] dismissed · `comment-noise` · `src/components/kosztorys/editor/hooks/use-inline-rename.ts:38` (", not for the render cost") · kontrast wyprzedza dokładnie tę edycję, którą czytelnik by tu zrobił (repo ma React Compiler + regułę „nie pisz ręcznie useCallback") — to jest wąski wyjątek „pułapki", nie narracja
- [x] dismissed · `module-cohesion` · `src/lib/kosztorys/row-content-lines.ts` · `wrapColumnClass` obok `WRAPPING_COLUMN_IDS` to przypadek „dopasowanej pary" — `use-wrap-column-widths.ts` importuje oba razem; rozdzielenie rozbija kontrakt kolumny zawijanej na dwa pliki, które muszą być zgodne
- [x] dismissed · `module-cohesion` · `src/components/kosztorys/editor/grid/cells/section-name-cell.tsx` · komórka + jej `Column` w jednym pliku to wzorzec wszystkich 9 rodzeństw w tym katalogu; diff nie dodał żadnego eksportu
- [x] dismissed · `code-review` · idempotencja `close()`, świeżość `commitRef`, `cancelledRef` vs unmount, pasmo przy `focus === undefined`, poszerzenie `WRAPPING_COLUMN_IDS`, casing CSS · sześć obszarów prześledzonych do końca, każdy czysty: `editingRef` zerowane PRZED commitem zamyka wyścig blur↔unmount; efekty rejestrowane w kolejności, więc `commitRef` nigdy nie jest nieaktualny; `isSyntheticRow` chroni stopki sekcji; selektory CSS matchują `sectionName` co do znaku
- [x] dismissed · `tailwind-v4-audit` · 0 trafień w trzech grupach (var() w klasie arbitralnej, inline `style`, wartości w nawiasach); diff nie dotyka żadnego breakpointa
- [x] dismissed · `feature-first-structure` · 0 nowych plików; `wrapColumnClass` importowany w dół (`components/` → `lib/`) jak już robi to `kosztorys-v2-columns.tsx` dla `description`/`note`; `useInlineRename` ma dwa katalogi-konsumentów, więc `editor/hooks/` to poprawny dom
- [x] dropped · `module-cohesion` · `src/lib/kosztorys/row-content-lines.ts` · nazwa pliku węższa niż temat modułu (`wrapping-columns.ts` pasowałoby lepiej) — realne, ale 6 importerów churnu za kosmetykę; zastane, nie wprowadzone przez ten diff

- [x] skipped · simplify(altitude) · `use-inline-rename.ts:12` · hook ma trzy osobne pokrętła „tylko w siatce" (`focus`, `stopEditing`, wyeksportowany `close` wpinany ręcznie w efekt po stronie komórki). Zwinięcie ich w jeden `grid?: { focus?, stopEditing }` z efektem wewnątrz hooka ZOSTAŁO ZROBIONE I COFNIĘTE: `react-hooks/set-state-in-effect` (React Compiler) nie przepuszcza synchronicznego setState w efekcie hooka, a przy komórce reguła nie widziała przez granicę hooka. Bez `focus` bag nie ma sensu, więc parametr został bary. Obejście = `eslint-disable` na regule pilnującej realnego kaskadowego renderu — nie za taką cenę i nie bez możliwości puszczenia testów. Powód zapisany w komentarzu przy sygnaturze.
- [x] fixed · simplify(reuse) · `ui/datasheet-grid/types.ts` · `(opts?: { nextRow?: boolean }) => void` przepisywany ręcznie w 4 plikach → jeden `StopEditingT = CellProps<unknown, unknown>['stopEditing']`, wzięty z typów dsg. Podbicie biblioteki nie rozjedzie się już po cichu.
- [x] fixed · simplify · `lib/table/column-order.ts:46` · ten sam przykład (kolumny rabatu pod rabatem globalnym) dwa razy w jednym akapicie, trzy linijki od siebie — usunięty wtręt.
- [x] fixed · simplify · `lib/kosztorys/row-content-lines.ts:17` · komentarz twierdził „dodanie kolumny zawijanej to jedna edycja"; nieprawda — `globals.css` nadal ręcznie wypisuje parę selektorów per id. Zapisane jako trzecia edycja, która gubi „…" po cichu.
- [x] fixed · simplify · `styles/globals.css:398` · inwentarz `kosztorys-clipped-*` w komentarzu powtarzał trzy selektory siedem linijek niżej.
- [x] fixed · simplify · `lib/kosztorys/column-config.ts:230` · komentarz odtwarzał definicję `ALL_PLANE_PRICE_KEYS` zamiast powodu jego istnienia.
- [x] fixed · simplify · `section-name-cell.tsx:29` · komentarz nad `stopEditing?` powtarzał samo `?`; ta sama teza stała w 4 miejscach w jednym diffie — została jedna, przy hooku.
- [x] fixed · simplify · `section-name-cell.test.tsx:24` · `renderGridCell` był drugą kopią `renderCell`, a dwa `rerender` trzecią i czwartą kopią tego samego drzewa; jeden `tree(props)` + `renderCell(props)`, trzy wołania `userEvent.setup()` wypadły same.
- [x] fixed · simplify · `use-inline-rename.test.tsx:12` · `renderHook`+`start` powtórzone 6×, atrapa zdarzenia klawiatury 3× → `mount()` / `keyEvent()`. Doszedł spec na nowe wyjście przez `focus` (hook przejął efekt, więc przejmuje i dowód).
- [x] fixed · simplify · `plane-price-columns.test.ts:23` · ta sama trójka widoków wypisana dwa razy → `VIEWS`.
- [x] fixed · simplify · `workshop-columns.test.ts:79` · dwa testy identyczne co do kształtu (`priceMode` vs `price`), których stała-para nie pozwala oblać osobno → jeden. Pętla dalej jedzie po `TOOL_PLANES`, nie po `ALL_PLANE_PRICE_KEYS` — inaczej klucz wypadający ze stałej po cichu skróciłby pętlę zamiast oblać test (propozycja agenta szła w drugą stronę, odrzucona).
- [x] fixed · simplify · `row-content-lines.test.ts:28` · nowy test „ukryta kolumna" dla `sectionName` to ta sama ścieżka co istniejący „ignores a column the client cannot see".
- [x] dismissed · simplify(reuse) · `section-name-cell.test.tsx:170` · rzekomo zbędne pokrycie: hook i komórka sprawdzają to samo oddanie siatce. Nie — regresja polegała dokładnie na tym, że komórka NIE przekazywała `stopEditing` dalej, a `renderHook` tego nie widzi. Oba zostają.
- [x] dropped · simplify(reuse) · `use-cell-draft.ts:104` · wspólna fabryka `gridHandoverKeys({stopEditing, onEnterExit, onEscapeExit})` dla obu hooków: parametry ≈ kod, a wyjścia i tak się różnią (`useCellDraft` na Escape woła `cancel()`, nie blur). Bez zysku.
- [x] dropped · simplify(efficiency) · `lib/kosztorys/row-content-lines.ts:7` · cache liczby linii przed `countWrappedLines`, bo `sectionName` powtarza się w każdym wierszu sekcji. Droga część (pomiar na canvasie) jest już cache'owana per słowo; zostaje split/filter na krótkim stringu. Nowa warstwa cache z pytaniem o czas życia za to nie płaci.
- [x] skipped · simplify(efficiency) · `kosztorys-editor-body.tsx:295` · `clipCueClass` liczy per wiersz to, co `contentLinesFor` policzył przed chwilą (drugi `measureTextWidth`, własne tablice). Kształt sprzed tego diffu — diff podniósł N z 2 na 3. Przewód pomiarowy edytora to osobny refaktor, nie sprzątanie po slice'ie.
- [x] skipped · simplify(reuse) · `use-inline-rename.ts:31` · trójka refów + strażnik idempotencji powiela mechanizm z `use-cell-draft.ts`; wspólny `useDraftExit(commit)` to refaktor wart własnego przeglądu, a nie doklejenie do tego gate'u.
- [x] skipped · simplify(altitude) · `styles/globals.css:406` · prawdziwy szew to przecięcie klasy wiersza z klasą kolumny w CSS; dsg przyjmuje `cellClassName` jako funkcję per wiersz, co zwinęłoby trzy reguły do jednej bez-id. Wymaga przeciągnięcia werdyktu z `kosztorys-editor-body.tsx` do fabryk kolumn — osobna zmiana. Fałszywą obietnicę w komentarzu naprawiono wyżej.

## Simplify pass

`/simplify` — 4 agentów (reuse / simplification / efficiency / altitude) na read-only snapshocie diffu.
11 fixed, 1 dismissed, 2 dropped, 4 skipped; każde ustalenie wpisane wyżej w `## Findings` (tag `simplify`).
Snapshot diffu: `<scratchpad>/slice.diff`. Osobnego raportu nie ma — ten plik jest raportem.

## Tests & suite

**Nie uruchomione — na wyraźne polecenie użytkownika** ("do not start tests etc another agent is
actively adding code", 2026-09-22). Nie puszczono ani `pnpm typecheck`, ani żadnego specu, ani suite.
Zmiany po `/simplify` są więc **niezweryfikowane maszynowo** — to jest dług tego gate'u, nie jego wynik.

Do odrobienia, gdy drzewo będzie wolne:

- `pnpm exec vitest run src/__tests__/components/kosztorys/editor/hooks/use-inline-rename.test.tsx`
- `pnpm exec vitest run src/__tests__/components/kosztorys/editor/grid/cells/section-name-cell.test.tsx`
- `pnpm exec vitest run src/__tests__/components/kosztorys/editor/grid/plane-price-columns.test.ts src/__tests__/components/kosztorys/editor/grid/workshop-columns.test.ts src/__tests__/lib/kosztorys/row-content-lines.test.ts`
- `pnpm typecheck` (podmiana `StopEditingT` dotknęła `use-cell-draft.ts` i `long-text-cell.tsx` poza diffem)
