diff --git a/dist/components/DataSheetGrid.js b/dist/components/DataSheetGrid.js
index ec561f6812c13bb948f23aa6f48ad16cf114c74d..20b5ad2e02c0870ca9acb2a04e34392dd6b6eb42 100644
--- a/dist/components/DataSheetGrid.js
+++ b/dist/components/DataSheetGrid.js
@@ -72,7 +72,7 @@ exports.DataSheetGrid = react_1.default.memo(react_1.default.forwardRef(({ value
     const afterTabIndexRef = (0, react_1.useRef)(null);
     // Default value is 1 for the border
     const [heightDiff, setHeightDiff] = (0, useDebounceState_1.useDebounceState)(1, 100);
-    const { getRowSize, totalSize, getRowIndex } = (0, useRowHeights_1.useRowHeights)({
+    const { getRowSize, totalSize, getRowIndex, resetAfter } = (0, useRowHeights_1.useRowHeights)({
         value: data,
         rowHeight,
     });
@@ -1265,6 +1265,9 @@ exports.DataSheetGrid = react_1.default.memo(react_1.default.forwardRef(({ value
             setSelectionMode({ columns: false, active: false, rows: false });
             setSelectionCell(null);
         },
+        resetRowHeights: (fromIndex) => {
+            resetAfter(Math.max(0, fromIndex !== null && fromIndex !== void 0 ? fromIndex : 0));
+        },
     }));
     const callbacksRef = (0, react_1.useRef)({
         onFocus,
diff --git a/dist/hooks/useRowHeights.js b/dist/hooks/useRowHeights.js
index 114f7825c4dc8bd5f4d921b26074e28018a02ce8..6e99a2fb25b4b1efb1ed8dd9535d81aef63f3eb9 100644
--- a/dist/hooks/useRowHeights.js
+++ b/dist/hooks/useRowHeights.js
@@ -42,7 +42,11 @@ const useRowHeights = ({ value, rowHeight, }) => {
                         top: lastBottom,
                     });
                     lastBottom += height;
-                } while (lastBottom <= top && r < calculatedHeights.current.length - 1);
+                    // Bounded by the DATA, not by the cache it is filling: `calculatedHeights.length`
+                    // is always `r + 1` right after the push above, so the original condition ended
+                    // the walk after a single row and left `totalSize` reporting one row's height
+                    // for the whole grid.
+                } while (lastBottom <= top && r < value.length - 1);
             }
             return r;
         };
@@ -61,8 +65,13 @@ const useRowHeights = ({ value, rowHeight, }) => {
                 if (index < calculatedHeights.current.length) {
                     return calculatedHeights.current[index];
                 }
-                let lastBottom = calculatedHeights.current[calculatedHeights.current.length - 1].top +
-                    calculatedHeights.current[calculatedHeights.current.length - 1].height;
+                // An empty cache (freshly reset — see the DataSheetGrid patch's `resetRowHeights`)
+                // has no last entry to read `.top`/`.height` off; mirror getRowIndex's own `r === -1`
+                // handling above and start the walk from 0 instead of crashing.
+                let lastBottom = calculatedHeights.current.length === 0
+                    ? 0
+                    : calculatedHeights.current[calculatedHeights.current.length - 1].top +
+                        calculatedHeights.current[calculatedHeights.current.length - 1].height;
                 for (let i = calculatedHeights.current.length; i <= index; i++) {
                     const height = rowHeight({ rowIndex: i, rowData: value[i] });
                     calculatedHeights.current.push({ height, top: lastBottom });
diff --git a/dist/types.d.ts b/dist/types.d.ts
index 52c4b80895fbb1398d87fb8880e6def17feaadc1..cf521cc0a35ceb45ecaa55425a40a07f41558026 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -193,6 +193,7 @@ export type DataSheetGridRef = {
     selection: SelectionWithId | null;
     setActiveCell: (activeCell: CellWithIdInput | null) => void;
     setSelection: (selection: SelectionWithIdInput | null) => void;
+    resetRowHeights: (fromIndex?: number) => void;
 };
 export {};
 //# sourceMappingURL=types.d.ts.map
